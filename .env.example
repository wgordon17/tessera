# ===== LLM Provider Configuration =====
# For 1Password integration: Comment out or remove API keys below.
# Framework automatically falls back to 1Password CLI if keys are not set.
# See "1Password Setup" section below for configuration instructions.

# Option 1: OpenAI API (Direct)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4-turbo-preview

# Option 2: GitHub Copilot Proxy (Recommended for free Copilot users)
# Set OPENAI_BASE_URL to use the Copilot proxy instead of direct OpenAI
# IMPORTANT: First run: npx copilot-api@latest auth
# OPENAI_BASE_URL=http://localhost:4141/v1
# GITHUB_TOKEN=ghu_your_copilot_token_here  # Must start with ghu_ (NOT ghp_!)

# Option 3: Anthropic API (Direct)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Option 4: Azure OpenAI
AZURE_OPENAI_API_KEY=your_azure_key_here
AZURE_OPENAI_ENDPOINT=your_azure_endpoint_here
AZURE_OPENAI_DEPLOYMENT=your_deployment_name_here

# ===== Agent Configuration =====
DEFAULT_TEMPERATURE=0.7
MAX_ITERATIONS=10
ENABLE_LOGGING=true

# ===== Rate Limiting & Retry Configuration =====
# Maximum number of retries on rate limit or network errors (default: 3)
MAX_RETRIES=3

# Request timeout in seconds (optional, no default)
# Uncomment to set a timeout:
# REQUEST_TIMEOUT=60.0

# ===== UV Configuration =====
# Configure UV to create venv in .cache/ directory (keeps root clean)
UV_PROJECT_ENVIRONMENT=.cache/.venv

# ===== Python Configuration =====
# Redirect all __pycache__ directories to .cache/ (keeps source dirs clean)
PYTHONPYCACHEPREFIX=.cache/pycache

# ===== 1Password Setup =====
# To use 1Password for secure secret storage:
#
# 1. Install 1Password CLI:
#    brew install 1password-cli
#
# 2. Sign in to 1Password:
#    eval $(op signin)
#
# 3. Get your secret reference from 1Password:
#    - Open 1Password app
#    - Navigate to your secret (e.g., GitHub Copilot token)
#    - Click the field you want to use
#    - Click "Copy Secret Reference" (looks like op://...)
#
# 4. Configure the op:// references below:
#    OP_GITHUB_ITEM=op://Private/GitHub-Copilot/copilot-token
#    (Note: Copilot token must be generated via: npx copilot-api@latest auth)
#    (Token format must be ghu_* NOT ghp_*)
#    OP_OPENAI_ITEM=op://Private/OpenAI-API/credential
#    OP_ANTHROPIC_ITEM=op://Private/Anthropic-API/credential
#
# 5. Comment out or remove the plaintext API keys above
#
# The framework will automatically read from 1Password if env vars are not set.
# Format: op://VaultName/ItemName/FieldName
