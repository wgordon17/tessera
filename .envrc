# direnv configuration for autonomy project
#
# First-time setup:
#   brew install direnv
#   echo 'eval "$(direnv hook zsh)"' >> ~/.zshrc  # or ~/.bashrc
#   direnv allow .
#
# After setup, direnv automatically loads .env when you cd into this directory.

# Load .env file if it exists
dotenv_if_exists

# Configure UV to create venv in .cache/ directory (keeps root clean)
export UV_PROJECT_ENVIRONMENT=.cache/.venv

# Redirect all __pycache__ directories to .cache/pycache
export PYTHONPYCACHEPREFIX=.cache/pycache
